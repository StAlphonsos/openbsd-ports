$OpenBSD$

diff -Nup mozilla-esr31/security/manager/ssl/src/nsCMS.cpp tor-browser/security/manager/ssl/src/nsCMS.cpp
--- security/manager/ssl/src/nsCMS.cpp.orig	Mon Jan  5 23:07:59 2015
+++ security/manager/ssl/src/nsCMS.cpp	Tue Feb 17 14:37:56 2015
@@ -264,9 +264,10 @@ nsresult nsCMSMessage::CommonVerifySignature(unsigned 
   NS_ENSURE_TRUE(certVerifier, NS_ERROR_UNEXPECTED);
 
   {
-    SECStatus srv = certVerifier->VerifyCert(si->cert, nullptr,
+    SECStatus srv = certVerifier->VerifyCert(si->cert,
                                              certificateUsageEmailSigner,
-                                             PR_Now(), nullptr /*XXX pinarg*/);
+                                             PR_Now(), nullptr /*XXX pinarg*/,
+                                             nullptr /*hostname*/);
     if (srv != SECSuccess) {
       PR_LOG(gPIPNSSLog, PR_LOG_DEBUG,
              ("nsCMSMessage::CommonVerifySignature - signing cert not trusted now\n"));
