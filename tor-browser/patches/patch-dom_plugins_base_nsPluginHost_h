$OpenBSD$

diff -Nup mozilla-esr31/dom/plugins/base/nsPluginHost.h tor-browser/dom/plugins/base/nsPluginHost.h
--- dom/plugins/base/nsPluginHost.h.orig	Mon Jan  5 23:08:04 2015
+++ dom/plugins/base/nsPluginHost.h	Tue Feb 17 14:37:55 2015
@@ -242,6 +242,8 @@ class nsPluginHost : public nsIPluginHost, (private)
   // Loads all cached plugins info into mCachedPlugins
   nsresult ReadPluginInfo();
 
+  PRBool GhettoBlacklist(nsIFile *pluginFile);
+
   // Given a file path, returns the plugins info from our cache
   // and removes it from the cache.
   void RemoveCachedPluginsInfo(const char *filePath,
