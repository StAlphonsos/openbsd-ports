$OpenBSD$

diff -Nup mozilla-esr31/security/manager/ssl/src/nsUsageArrayHelper.cpp tor-browser/security/manager/ssl/src/nsUsageArrayHelper.cpp
--- security/manager/ssl/src/nsUsageArrayHelper.cpp.orig	Mon Jan  5 23:07:59 2015
+++ security/manager/ssl/src/nsUsageArrayHelper.cpp	Tue Feb 17 14:37:56 2015
@@ -105,8 +105,9 @@ nsUsageArrayHelper::check(uint32_t previousCheckResult
     MOZ_CRASH("unknown cert usage passed to check()");
   }
 
-  SECStatus rv = certVerifier->VerifyCert(mCert, nullptr, aCertUsage,
-                         time, nullptr /*XXX:wincx*/, flags);
+  SECStatus rv = certVerifier->VerifyCert(mCert, aCertUsage, time,
+                                          nullptr /*XXX:wincx*/,
+                                          nullptr /*hostname*/, flags);
 
   if (rv == SECSuccess) {
     typestr.Append(suffix);
