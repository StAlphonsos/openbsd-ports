$OpenBSD$

diff -Nup mozilla-esr31/netwerk/protocol/http/nsHttpConnectionInfo.cpp tor-browser/netwerk/protocol/http/nsHttpConnectionInfo.cpp
--- netwerk/protocol/http/nsHttpConnectionInfo.cpp.orig	Mon Jan  5 23:07:57 2015
+++ netwerk/protocol/http/nsHttpConnectionInfo.cpp	Tue Feb 17 14:37:56 2015
@@ -107,6 +107,11 @@ nsHttpConnectionInfo::SetOriginServer(const nsACString
         mHashKey.Append(':');
         mHashKey.AppendInt(ProxyPort());
         mHashKey.Append(')');
+        mHashKey.Append('[');
+        mHashKey.Append(ProxyUsername());
+        mHashKey.Append(':');
+        mHashKey.Append(ProxyPassword());
+        mHashKey.Append(']');
     }
 }
 
