$OpenBSD$

diff -Nup mozilla-esr31/dom/base/nsPerformance.cpp tor-browser/dom/base/nsPerformance.cpp
--- dom/base/nsPerformance.cpp.orig	Mon Jan  5 23:08:02 2015
+++ dom/base/nsPerformance.cpp	Tue Feb 17 14:37:55 2015
@@ -39,7 +39,12 @@ nsPerformanceTiming::nsPerformanceTiming(nsPerformance
 {
   MOZ_ASSERT(aPerformance, "Parent performance object should be provided");
   SetIsDOMBinding();
-  // The aHttpChannel argument is null if this nsPerformanceTiming object
+
+  if (!nsContentUtils::IsPerformanceTimingEnabled()) {
+    mZeroTime = 0;
+  }
+
+ // The aHttpChannel argument is null if this nsPerformanceTiming object
   // is being used for the navigation timing (document) and has a non-null
   // value for the resource timing (any resources within the page).
   if (aHttpChannel) {
